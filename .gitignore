.DS_Store
node_modules/

# secrets — never commit
.env
.env.*
!.env.example
# 확장 OpenAI 키 설정 (절대 커밋·번들 금지 — 평문 노출)
chrome-extension/config.js

# Next.js
.next/
*.tsbuildinfo
next-env.d.ts

# SQLite dev DB + 수동 백업
*.db
*.db-journal
*.db.bak*
# 정제 작업용 카탈로그 시드 백업
*.json.bak*

# 일회성/생성 카탈로그 데이터 산출물 (실제 데이터는 dev.db 에 있음)
/channels-final.json
/dump.sql
# 발굴 중간 산출물 (경로 무관 — jhs/discovered-pending.json 등 포함)
discovered-*.json
jhs/data/sub-taxonomy.proposed.json

# 발표 자료(docs) — 생성물/원본 백업/임시 스크립트는 커밋 제외(HTML+최적화 이미지만 게시)
docs/*.pdf
docs/shots/_orig/
docs/extension/_orig/
docs/shoot.mjs
