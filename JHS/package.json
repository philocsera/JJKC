{
  "name": "jhs-phase2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 2 — exploring others' YouTube algorithms. Ports-and-adapters core, decoupled from prototype.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "test:watch": "node --import tsx --test --watch \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
